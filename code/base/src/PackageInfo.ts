// LATER: These values will be set in pre-build step
export const NAME = ((null as unknown) as string);
export const VERSION = ((null as unknown) as string);
export const BUILD_DATE = ((null as unknown) as string);
